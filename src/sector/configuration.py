from typing import Any


def load(path: str) -> dict[Any, Any]:

    # FIXME: This should be loading a file at some point
    config = {"mapper": {"console-plugin": "kuadrant-console-plugin"}}
    return config
